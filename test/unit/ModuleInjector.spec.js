const path = require('path');
const chalk = require('chalk');

describe(chalk.magenta('Unit: ModuleInjector'), () => {

  it.skip('Should load a file as a destination attribute', (done) => {

    const destination = {};
    const partialLoadPath = 'test/unit/_resoures/fake-module/fake-file.js';

  });

  it.skip('Should load a folder as a destination object attribute', (done) => {

  });

});
